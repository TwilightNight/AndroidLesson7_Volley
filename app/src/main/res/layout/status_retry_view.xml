<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/status_retry_view_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <Button
        android:id="@+id/status_retry_view_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="Retry"/>
    <TextView
        android:id="@+id/status_retry_view_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/status_retry_view_button"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:text="Connection fail click button to retry."/>
</RelativeLayout>
